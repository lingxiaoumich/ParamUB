"""Parametric car underbody (floor + diffuser + wheel houses + 4 wheels).

Builds on top of the ParamUB wheel generator (tire_assembly.py) by importing
its build functions and placing 4 wheels at the corners of the wheelbase
x track-width rectangle, with per-axle camber/toe.
"""
